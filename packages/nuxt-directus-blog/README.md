# @croffledev/nuxt-directus-blog

Nuxt Layer 패키지입니다. Nitro `/api/*` 라우트와 composable을 제공하고, `@croffledev/directus-blog-core`를 사용합니다.

## 사용

```ts
// nuxt.config.ts
export default defineNuxtConfig({
  extends: ['@croffledev/nuxt-directus-blog'],
});
```

현재는 스캐폴드만 있습니다.
